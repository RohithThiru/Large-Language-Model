[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "Large-Language_model"
version = "0.1.0"
description = "A small language model built from scratch"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]